# 2-Learning-Project-Simple-CNN-Model
This project implements a simple Convolutional Neural Network (CNN) for image classification. The model learns visual patterns from images using convolutional and fully connected layers. It demonstrates the basics of deep learning, including data preprocessing, model training, and prediction using Python and deep learning libraries.
